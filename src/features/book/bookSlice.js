import { createSlice } from '@reduxjs/toolkit';

const initialState = {};

const bookSlice = createSlice({
  name: 'book',
  initialState,
  reducers: {
    // Add reducers here
  },
});

export default bookSlice.reducer;
